# FFT CoProcessor 

## Architecture Overview 
 1. Chipyard's Rocket core paired with an FFT co-processor (both configure eachother's CSRs) 
 2. FFT co-processor is configurable through address-mapped config registers 
 3. Process Node = Skywater 130A 

